<h1><%= @task.title %></h1>
<p><%= @task.details %></p>
<p><%= @task.completed == true ? "Completed" : "Not completed" %></p>
<p><%= link_to "Edit task", edit_path %></p>
<p><%= link_to "Delete task", destroy_path, method: :delete,data: { confirm: "Are you sure?" } %></p>
<p><%= link_to "Back to list", tasks_path %></p>
